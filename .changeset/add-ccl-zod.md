---
"ccl-zod": minor
---

Add ccl-zod package for schema-based typed extraction from CCLObjects using Zod schemas. Includes `extract()` function, `cclBoolean()` custom schema, and structured error types.

---
"ccl-test-runner-ts": minor
---

Default to version from .version file when no --version flag is passed. Add --latest flag to explicitly fetch the latest release, ignoring any pinned .version file.
